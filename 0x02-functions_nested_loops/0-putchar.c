#include "main.h"

/**
  * main - Program to print _putchar on the std output
  * Return: Always 0.
  */

int main(void)
{
	char ch;

	ch = '_';
	putchar(ch);
	ch = 'p';
	putchar(ch);
	ch = 'u';
	putchar(ch);
	ch = 't';
	putchar(ch);
	ch = 'c';
	putchar(ch);
	ch = 'h';
	putchar(ch);
	ch = 'a';
	putchar(ch);
	ch = 'r';
	putchar(ch);
	putchar('\n');
	return (0);
}
