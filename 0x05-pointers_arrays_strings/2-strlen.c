#include <stdio.h>
#include <string.h>

/**
  * _strlen - Returns the length of a string
  * Return: the lenght of @*s
  * @s: The string to be checked
  */

int _strlen(char *s)
{
	return (strlen(s));
}
