#include <stdio.h>

/**
  * main - entrance point the program
  * Return: zero if no error and non-zero if error is encountered
  */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	return (0);
}
